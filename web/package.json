{
    "name": "web",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite dev --host",
        "build": "vite build --emptyOutDir",
        "preview": "vite preview",
        "format": "oxfmt --write .",
        "lint": "oxfmt --check . && oxlint --type-aware --type-check ."
    },
    "devDependencies": {
        "@types/leaflet": "^1.9.21",
        "leaflet": "^1.9.4",
        "oxfmt": "^0.60.0",
        "oxlint": "^1.75.0",
        "oxlint-tsgolint": "^7.0.2001",
        "vite": "^8.1.5"
    }
}
